# RecoverIQ Web

This Next.js App Router application is the RecoverIQ operational shell. Run `npm install` and `npm run dev`; it reads `NEXT_PUBLIC_API_BASE_URL` at build time and defaults to `http://localhost:8000`. The current Command Center displays only API/runtime foundation status and deliberately contains no fabricated recovery metrics.

